'use strict';

/**
* Manage the queries for the level model
**/
module.exports = {
	/**
	* Get all the levels
	**/
	signing: async () => {
    const token = fastify.jwt.sign({ payload });
    return token;
	}
};
